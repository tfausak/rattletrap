module HLint
  (
  ) where

import "hint" HLint.Dollar
import "hint" HLint.HLint

ignore "Avoid lambda"

ignore "Use ***"

ignore "Use &&&"

ignore "Use <|>"

ignore "Use first"

ignore "Use import/export shortcut"
